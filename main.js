const tilt = $('.debit-card').tilt({
    maxTilt:        15,
    easing:         "cubic-bezier(.03,.98,.52,.99)",
    speed:          500, 
    glare:         true,
    perspective:    1500, 
    maxGlare:       0.4,
}
);
